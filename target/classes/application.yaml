spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql:///loginprojectdemo?characterEncoding=utf-8&serverTimezone=GMT%2B8&useSSL=false&&allowPublicKeyRetrieval=true
    username: root
    password: 1234

  web:
    resources:
      static-locations: classpath:/static/

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
